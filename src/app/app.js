var $ = require('jquery');

function start() {
  // Just get ri

  // Start the app here
  $('#status').html('If you can read this text, your stack should be alright.');
}

exports = module.exports = {
  start: start
};
